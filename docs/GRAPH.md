# README Activity Graph

The profile activity graph in `README.md` is rendered by GitHub Readme Activity Graph:

- URL: `https://github-readme-activity-graph.vercel.app/graph`
- Parameters include `username`, `theme`, `days`, `grid`, `bg_color`, and `custom_title`.

To update the graph, change the `username` or theme parameters directly in the image URL.
